import React from 'react';
import sample from "./sample.svg";

export default () => {
    return (
        <div>
            <img src={sample} alt="" style={{width: '20px', margin: "auto", display: 'block'}}/>
        </div>
    );
};