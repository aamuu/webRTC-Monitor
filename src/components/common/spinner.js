import React from 'react';
import sample from "./sample.svg";

export default () => {
    return (
        <div>
            <img src={sample} alt="" style={{width: '40px', margin: "auto", display: 'block'}}/>
        </div>
    );
};